# 비밀/자격증명 — 실제 값은 절대 커밋 금지. 템플릿(*.example)만 추적.
.env
.env.*
!.env.example
deploy.env
deploy.env.*
backend/.env
backend/.env.*
!backend/.env.example
*.pem
*.key
secrets.*

# 배포 관련 (비공개 — 로컬/서버에만 존재, git 추적 안 함)
deploy.sh
DEPLOY.md
docker-compose.prod.yml
.env.prod.example
deploy.env.example

# 파이썬
__pycache__/
*.pyc
backend/.venv/
.pytest_cache/

# 데이터/로그
*.db
data/
*.log

# OS/에디터
.DS_Store

# 자격증명 보관 (로컬 전용, 절대 커밋 금지)
.secrets/
