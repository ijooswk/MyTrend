# 원격 배포 설정/자격증명 템플릿.
# 이 파일을 deploy.env 로 복사해 값을 채우세요. (deploy.env 는 .gitignore 처리됨)
# SSH 키를 등록(ssh-copy-id)하면 SERVER_PASSWORD 없이 동작합니다.

SERVER_IP=100.98.178.217
SERVER_USER=sehun
SERVER_PASSWORD=          # 비우면 SSH 키 인증 사용(권장). 비밀번호 쓰면 로컬에 sshpass 필요
SSH_PORT=22
REMOTE_DIR=/home/sehun/mytrend
